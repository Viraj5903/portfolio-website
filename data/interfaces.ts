/**
 * @interface NavLink
 * Represents a navigation link in the application.
 * 
 * @property {string} name - The display name of the navigation link.
 * @property {string} href - The URL or anchor link the navigation link points to.
 */
export interface NavLink {
    name: string;
    href: string;
};